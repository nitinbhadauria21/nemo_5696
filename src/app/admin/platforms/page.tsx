export default function AdminPlatformsPage() {
  return (
    <div className="admin-card p-6">
      <h2 className="font-display text-lg font-bold">Platform Heatmap</h2>
      <p className="mt-2 text-sm text-[var(--admin-mute)]">
        Cross-platform usage intensity by niche and region — coming in v2.
      </p>
    </div>
  );
}
